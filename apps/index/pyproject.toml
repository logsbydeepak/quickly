[project]
name = "index"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["db", "psycopg[binary]>=3.3.3"]

[tool.uv.sources]
db = { workspace = true }
